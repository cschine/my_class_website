---
title: "In-Class Midterm Prep"
subtitle: 'Tuesday: Cracking open and Exploring New Data'
author: "Casey Schine"
format: html 
execute: 
  message: FALSE
  warning: FALSE
editor_options: 
  chunk_output_type: console
---

# Midterm Project: Exploratory Data Analysis

Today we are going to begin working toward the **midterm project**. Over the next two classes we will start the process of **exploratory data analysis (EDA)** using the datasets that you will use for the project.

For the midterm, you will:

-   explore a dataset
-   investigate one interesting question
-   create a few visualizations
-   describe the patterns you observe

Your report will include three main sections:

1.  **Introduction** – What dataset are you using and what question are you asking?
2.  **Results** – What patterns do you observe in the data?
3.  **Discussion** – What might those patterns mean?

I always think about these sections as answering the questions:

1.  What do we already know and why do we care?
2.  What did you find?
3.  What do you think?

The work we do during today's class and the next class will help you begin this process.

# Dataset Options

For this assignment you will choose **one of two datasets**.

### Environmental Justice Index (EJI)

The Environmental Justice Index combines environmental, demographic, and health data to measure environmental burdens experienced by communities across the United States. The data are organized at the level of **census tracts**, which are small geographic areas used by the U.S. Census Bureau.

The dataset includes information about environmental exposures, social vulnerability, and health outcomes.

Some questions someone might explore with this dataset include:

-   Which regions experience the highest environmental burden?
-   Do some environmental indicators tend to occur together?
-   Are there geographic patterns in environmental justice measures?

### Youth Risk Behavior Survey (YRBS)

The Youth Risk Behavior Survey collects information from high school students about behaviors related to health and well-being. The survey includes questions about things like physical activity, mental health, substance use, and other behaviors.

The dataset also includes demographic information such as grade level and sex.

Some questions someone might explore with this dataset include:

-   Do certain behaviors differ across grade levels?
-   Are some behaviors related to others?
-   Are there differences between demographic groups?

# Choosing a Dataset

Before we begin exploring the data, take a moment to decide which dataset you would like to work with for your project.

You do **not** need to analyze the entire dataset. Most good analyses focus on a small number of variables.

Let's read in the data:

```{r}
#code to read in data
getwd()
# EJI
library(sf)
eji_data <- st_read("~/Dropbox/!Middlebury/Teaching/STAT 118/S2026/S2026_R_Proj/midterm/eji_sf.gpkg")

# YRBS
yrbs_data <- read_csv("~/Dropbox/!Middlebury/Teaching/STAT 118/S2026/S2026_R_Proj/midterm/yrbss_2024.csv")
```

[***Take a minute to decide which dataset you will use. You can change your mind later, so don't overthink it.***]{style="color:green;"}

## Exploratory Data Analysis

The process we will practice today is called **exploratory data analysis (EDA)**.

EDA is the process of using summaries and visualizations to discover patterns in data and develop questions about those patterns.

::: callout-note
## Useful quote from a random person on Reddit (onearmedecon)

The primary goal of EDA is to explore the underlying structure, patterns, relationships, and anomalies within the data, allowing the analyst to make informed decisions and develop appropriate models or strategies for further analysis.

This usually involves some combination of the following:

1.  Cleaning (e.g., removing duplicates)
2.  Transformations (e.g., aggregation)
3.  Descriptive/univariate analysis (e.g., summary statistics)
4.  Correlational/bivariate analysis (e.g., correlation matrices, simple hypothesis testing)
5.  Visualizations (e.g., histograms, scatter plots)
:::

**The workflow usually looks something like this:**

→ Understand the dataset\
→ Ask questions\
→ Create plots\
→ Identify patterns\
→ Interpret what those patterns might mean

This process is **iterative**.

![](https://r4ds.had.co.nz/diagrams/data-science-explore.png){fig-align="center"}

Today we will walk through the first part of this process.

## Step 1: Understanding the Dataset

Before we make any plots, we need to understand what the dataset contains.

Some useful questions to ask when you first encounter a dataset include:

-   What does each **row** represent?
-   What **variables** are included in the dataset?
-   What do those variables measure?
-   What if any data cleaning is needed?
-   Which variables seem interesting to explore?

Let's begin by examining the structure of the dataset.

```{r}
# ways we can look at the data structure
```

[***Group Discussion: Break into small groups and discuss these questions:***]{style="color:green;"}

-   What does each **row** represent?
-   What **variables** are included in the dataset?
-   What do those variables measure?
-   What if any data cleaning is needed?
-   Which variables seem interesting to explore?

[*Write down two or three variables that your group thinks might be interesting to explore.*]{style="color:green;"}

```{r}
# additional ideas for looking at the data
```

## Step 2: Brainstorming Questions

Now that we have looked at the structure of the dataset, the next step is to think about questions we might ask.

**Some common types of exploratory questions include:**

-   Do groups differ from one another?
-   Are two variables related?
-   What are the highest or lowest values?
-   How does something change across categories?

### [Group Task]{style="color:green;"}

[With your come up with one or two questions that someone might investigate using this dataset. Your questions do not need to be perfect. The goal is simply to identify something interesting to explore. After a few minutes we will collect some questions from the class.]{style="color:green;"}

## Step 3: Matching the Question with the Visualization

Once we have a question, the next step is to think about what kind of visualization might help answer that question.

Different kinds of questions are best answered by different kinds of plots.

| Question type                  | Useful plot       |
|--------------------------------|-------------------|
| Comparing groups               | Bar chart         |
| Relationship between variables | Scatterplot       |
| Distribution of values         | Histogram         |
| Ranking values                 | Ordered bar chart |

[**Let's pick one of the questions from the class and discuss what type of visualization might help us explore it.**]{style="color:red;"}

```{r}
#variables to explore smoke v. cancer
#E_SMOKE
#E_CANCER
eji_data |>
  #filter(STATE_NAME=="California") |>
  ggplot() +
  geom_point(aes(x=E_SMOKE, y=E_CANCER), alpha=0.2) +
  #scale_x_log10() +
  theme_tufte()

eji_data |>
  filter(E_SMOKE>15) |>
  ggplot(aes(x=E_SMOKE, y=E_CANCER)) +
  geom_hex(bins=50) +
  geom_smooth(color="red") +
  scale_fill_viridis_c() +
  #scale_x_log10() +
  theme_tufte()

eji_data |>
  #filter(E_SMOKE>15) |>
  ggplot(aes(x=E_BURN, y=E_CANCER)) +
  geom_hex(bins=50) +
  geom_smooth(color="red") +
  scale_fill_viridis_c() +
  scale_x_log10() +
  theme_tufte()

eji_data |>
  filter(STATE_NAME=="California") |>
  ggplot() +
  geom_point(aes(x=E_SMOKE, y=E_CANCER), alpha=0.2) +
  theme_tufte()
  
```

## Step 4: Create an Exploratory Plot

Create an exploratory plot to investigate the question chosen

### By Hand!!!

I'm a big meanie so I'm going to make you draw out your first plot idea by hand.

### [*First on your own then share with your group.*]{style="color:green;"}

### [***Let's draw some together!***]{style="color:red;"}

### Now code the plot in R.

### [*First on your own then consult with your group.*]{style="color:green;"}

*Your first plots do not need to be perfect. Exploratory plots are tools for helping us see patterns in the data.*

### [***Let's code some together!***]{style="color:red;"}

```{r}
# code up some exploratory plots
```

## Step 5: Identify and describe patterns

When we look at a plot, the first step is to describe what we see.

Examples of descriptive observations include:

-   One group has higher values than another group
-   The values increase across categories
-   There appears to be a relationship between two variables
-   Try to focus on what the data show, rather than explaining why.

[**As a group, discuss the patterns in the plots we just made:**]{style="color:green;"}

-   What does the plot show?
-   What doesn't it show?
-   What questions does it answer?
-   What questions does it raise?

## Step 6: Interpreting meaning

After identifying a pattern, we can begin to think about what it might mean.

Possible questions to consider:

-   What might explain the pattern we see?
-   Are there limitations in the data that affect our interpretation?
-   What additional questions does this pattern raise?

These kinds of ideas are what you will discuss in the Discussion section of your report.

[***Discuss answers to these questions with your group.***]{style="color:green;"}
