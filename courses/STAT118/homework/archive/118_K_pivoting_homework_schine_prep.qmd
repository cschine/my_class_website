---
title: "STAT 118: Homework J"
subtitle: 'reshaping data with `pivot_longer()` and `pivot_wider()`'
author: "YOUR NAME HERE"
format:
  html: 
    toc: true
    code-overflow: wrap
    code-fold: true
    embed-resources: true
---

```{r message=FALSE, warning=FALSE}
#LOAD PACKAGES 
library(tidyverse)
```

# Religious Income Data

The Pew Religious Income Survey, conducted in 2007 and 2014, surveys more than 35,000 Americans from all 50 states about their religious affiliations, beliefs and practices, and social and political views. This data comes from 2009.

```{r}
data("relig_income") # contained in tidyverse package
```

This dataset contains three variables:

-   religion of the surveyee
-   the annual income bracket of the surveyee
-   the count (number of surveyees)

# 1.

Is this data in wide or long format?

# 2.

Pivot the data (If it's in long format, make it wide. If it's in wide format, make it long).

```{r}

relig_income_long <- relig_income |>
  pivot_longer(cols=`<$10k`:`Don't know/refused`,
               names_to = "income",
               values_to = "count")

```

# Fish Encounters Dataset

The `fish_encounters` dataset contains information about fish swimming down a river. Each station recorded if a tagged fish was observed at its monitor stations. Fish travel is one direction (migrating downstream).

```{r}
data("fish_encounters") # contained in tidyverse package
```

The dataset contains three variables:

-   fish, the fish identifier,
-   station, the measurement stations
-   seen = 1 if the fish was seen

# 3.

Is this data in wide or long format?

# 4.

Pivot the data (If it's in long format, make it wide. If it's in wide format, make it long).

```{r}
fish_encounter_wide <- fish_encounters |>
  pivot_wider(names_from=station,
              values_from=seen,
              values_fill=0)

```

# 5.

You might notice that there are a lot of NA or missing values after pivoting wider. This is means that the fish was not observed at that given station. Let's replace the NA values with 0s. Repeat your pivot in the previous problem but, fill in the missing values with zeros.

```{r}

```

# `gapminder` Data

```{r}
library(gapminder)
data('gapminder')
```


# 6.

Recreate the following table from the `gapminder` dataset. Focus on the content first. If you have time, you can try to match the styling.

![](https://github.com/emalcolmwhite/STAT0118/blob/main/J_Pivoting/images/gapminder_table.jpg?raw=true)

```{r}

gapminder |>
  filter(year %in% c(1957, 2007)) |>
  filter(country %in% c("Afghanistan", "Brazil", "China")) |>
  select(country, year, gdpPercap) |>
  pivot_wider(names_from=year,
              values_from=gdpPercap)


```
# 7.

Recreate the following table from the `gapminder` dataset. Focus on the content first. If you have time, you can try to match the styling. *Hint: you will have to reshape it twice*

![](https://github.com/emalcolmwhite/STAT0118/blob/main/J_Pivoting/images/gapminder_us_table.png?raw=true)

```{r}
gapminder |>
  filter(year %in% c(2002, 2007)) |>
  filter(country=="United States") |>
  select(year, lifeExp, pop, gdpPercap) #|>
  pivot_longer(cols=lifeExp:gdpPercap,
               names_to="Statistic", 
               values_to = "Values") |>
  pivot_wider(names_from=year,
              values_from=Values)

```